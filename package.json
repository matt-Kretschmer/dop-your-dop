{
  "name": "dop-your-dop",
  "version": "1.0.0",
  "description": "NRDB",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-api": "cd api && npm start",
    "build-api": "cd api && npm install",
    "start-webapp": "cd webapp-v2 && npm run start",
    "build-webapp": "cd webapp-v2 && npm install && npm run build"
  }
}
